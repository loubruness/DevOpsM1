### Start command:
```docker-compose up --build```

### Stop command:
```docker-compose down```

### Find first password:
```docker exec jenkins-master cat /var/jenkins_home/secrets/initialAdminPassword```